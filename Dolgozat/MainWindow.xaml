<Window x:Class="Dolgozat.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dolgozat"
        mc:Ignorable="d"
        Title="Dolgozat" Height="450" Width="750"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid x:Name="DolgozatDataGrid"
                  Grid.Row="0"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="All"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  RowHeaderWidth="0"
                  ItemsSource="{Binding Dolgozatok}"
                  Margin="0,0,0,12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nev" Binding="{Binding Nev}" Width="2*"/>
                <DataGridTextColumn Header="Eletkor" Binding="{Binding Eletkor}" Width="*" />
                <DataGridTextColumn Header="Pontszam" Binding="{Binding Pontszam}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="Név:" Margin="0,0,6,0"/>
            <TextBox x:Name="NevTextBox" Grid.Row="0" Grid.Column="1" Height="24"/>

            <TextBlock Grid.Row="0" Grid.Column="3" VerticalAlignment="Center" Text="Életkor:" Margin="0,0,6,0"/>
            <TextBox x:Name="EletkorTextBox" Grid.Row="0" Grid.Column="4" Height="24"/>

            <TextBlock Grid.Row="0" Grid.Column="6" VerticalAlignment="Center" Text="Pontszám:" Margin="0,0,6,0"/>
            <TextBox x:Name="PontszamTextBox" Grid.Row="0" Grid.Column="7" Height="24"/>

            <Button Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Content="Hozzáadás" Width="120" Height="32" Margin="0,12,0,0" Click="Hozzaadas_Click"/>
            <Button Grid.Row="1" Grid.Column="4" Grid.ColumnSpan="2" Content="Mentés" Width="120" Height="32" Margin="0,12,0,0"/>
        </Grid>
    </Grid>
</Window>
